[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader = { version = "0.11.15" }
spin = { version = "0.10.0", default-features = false }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"