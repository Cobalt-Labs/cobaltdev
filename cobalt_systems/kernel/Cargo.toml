[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader_api = "0.11.15"
spin = { version = "0.10.0", features = ["mutex", "lazy"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"